# Notebook preservation

Place the author's exact final Colab notebook(s) here for archival release after running the finalizer. The finalizer searches the live Drive for Cell 60A–60E and row/group-ID construction cells and writes verbatim extracts into `src/`.
